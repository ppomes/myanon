secret = 'my_secret_key'
pypath = 'tests'
pyscript = 'test_row_context'

tables = {
  `user` = {
    `email`     = pydef 'anonymize_email'
    `firstname` = pydef 'anonymize_name'
    `lastname`  = pydef 'anonymize_name'
  }
}
