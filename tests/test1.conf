# Config file for test1.sql
secret = 'lapin'
stats  = 'yes' # include stats at the end of output

tables = {
   `lottypes` = {
     `int1`  = inthash 2
     `text1` = texthash 5
   }
   `tata` = {
     `id`    = inthash 2
     `email` = emailhash 'example.com' 10
   }
  `toto` = {
     `a`    = inthash 5
     `b`    = fixed 'valeurfixe'
     `name` = texthash 10
   }
}
