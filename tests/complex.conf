# Config file for complex.sql
secret = 'lapin'
stats  = 'no'

tables = {
   `points` = {
     `name`      = texthash 5
     `owner`     = json {
                          'email'     = emailhash 'example.com' 10
                          'last_name' = texthash 5
                          'first_name'= texthash 5
                        }
   }
}
