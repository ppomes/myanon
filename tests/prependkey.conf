# Config file for concat.sql
secret = 'lapin'
stats  = 'no'

tables = {
   `players` = {
     `id`   = key                     # Keep this field as key
     `name` = prependkey 'player'     # concat key and string 'player'
   }
}
