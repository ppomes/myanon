# Config file for test_long_table_name.sql
# The table name is 64 characters, the maximum MySQL allows. With the
# backticks it exceeds ID_SIZE: the scanner must truncate, not overflow.
secret = 'lapin'
stats  = 'no'

tables = {
   `tttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttt` = {
     `name`  = texthash 10
     `email` = emailhash 'example.com' 10
   }
}
