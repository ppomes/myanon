# Config file for test2.sql
secret = 'lapin'
stats  = 'yes'

tables = {
  `example` = {
     `example_id`    = inthash 5
     `name`          = fixed 'nom123'
     `value`         = texthash 10
   }
}
