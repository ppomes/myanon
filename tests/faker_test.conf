stats    = 'no'
pypath   = './tests'
pyscript = 'faker_test'

tables = {
   `contacts` = {
     `firstname` = pydef 'firstname'
     `lastname`  = pydef 'lastname'
     `email`     = pydef 'email'
   }
}
